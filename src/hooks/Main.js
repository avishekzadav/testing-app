import React from 'react'
import Props from './Props'

const Main = () => {
    return (
        <div>
            <Props language={'React'} technology={'web'}/>
            <Props language={'React-Native'} technology={'mobile'}/>
        </div>
    )
}

export default Main
